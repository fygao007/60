# 无数据表格页模板

来源：`mastergo://getd2c/195991886967017-2-016401`

原始 D2C 落盘：

- HTML：`.mastergo/195991886967017-2-016401/195991886967017-2-016401.html`
- 图片与图标：`.mastergo/195991886967017-2-016401/asset/`

这份文档是操作区“无数据表格页”的实现基准。业务文案和列定义可以替换，结构、组件状态、尺寸与间距应优先复用。

## 页面结构

1. 页面画布：`1196px × 820px`，四周 `12px`。
2. 操作区卡片：纵向布局，圆角 `12px`，内容背景 `#FFFFFF`，溢出隐藏。
3. 筛选区：渐变背景，顶部左右 `16px`、底部 `8px`。
4. 主内容区：左右 `16px`，工具栏与表格纵向间距 `12px`。
5. 表格区：表头、空状态内容，边框 `1px solid #EEEEF0`。
6. 分页区：固定高度 `64px`，内边距 `12px 16px`。

## 组件映射

| 模板区域 | 设计系统组件 | 本模板配置 |
|---|---|---|
| 筛选容器 | `DSFilterBar` | 多字段、可换行、展开状态 |
| 筛选字段 | `DSField` + `DSInput` | 标签右对齐，输入框默认空值 |
| 筛选辅助操作 | `DSIconButton` | 仅图标的默认态按钮 |
| 重置/查询 | `DSButton` | 次按钮 + 主按钮 |
| 展开控制 | `DSButton variant="link"` | 主色文字 + 向上箭头 |
| 表格操作区 | `DSDataTable.toolbarActions` | 普通按钮、带下拉箭头按钮 |
| 表格工具 | `DSDataTable.tableTools` / `DSIconButton` | 两个无边框图标按钮 |
| 表格 | `DSDataTable` | 复选框列、5 个业务列、空数据 |
| 空状态 | `DSDataTable.emptyRender` / `DSEmptyState` | 100px 图片 + “暂无数据” |
| 分页 | `DSPagination` | 总数、每页条数、页码、跳页 |

## 设计稿已出现的组件状态

| 组件 | 状态 | 视觉规则 |
|---|---|---|
| 输入框 | 默认 / 空值 | 高 `32px`，白底，`#DCDCE0` 边框，`4px` 圆角，占位文字 `#9697A3` |
| 图标按钮 | 默认 | `32px × 32px`，白底，`#DCDCE0` 边框，`4px` 圆角 |
| 次按钮 | 默认 | 高 `32px`，白底，黑字，`#DCDCE0` 边框，`4px` 圆角 |
| 主按钮 | 默认 | 高 `32px`，背景 `#333FFF`，白字，`4px` 圆角 |
| 下拉按钮 | 默认 | 次按钮样式，文字后接 `14px` 向下箭头 |
| 展开控制 | 已展开 | 文字 `#333FFF`，显示“展开”和 `14px` 向上箭头 |
| 表格复选框 | 未选中 | 表头首列显示 `14px` 未选中复选框 |
| 表格 | 空数据 | 保留表头，数据区使用居中空状态 |
| 空状态 | 默认 | `100px × 100px` 图片，下方 `8px` 为“暂无数据” |
| 分页页码 | 当前页 | `24px × 24px`，背景 `#EEEEF0`，字重 `600` |
| 分页页码 | 默认页 | `24px × 24px`，透明背景，文字 `#505266` |

设计稿没有展示 Hover、Focus、Pressed、Disabled、Loading、校验错误、半选、全选等状态。本模板不把这些状态的具体视觉值当作已确认规范；实现时继续沿用对应基础组件现有状态。

## 尺寸与间距

| 项目 | 数值 |
|---|---:|
| 页面外边距 | `12px` |
| 操作区水平内边距 | `16px` |
| 筛选区内边距 | `16px 16px 8px` |
| 筛选字段间距 | `8px` |
| 字段标签宽度 | `94px` |
| 标签与控件间距 | `8px` |
| 筛选字段最小宽度 | `240px` |
| 筛选字段最大宽度 | `600px` |
| 输入框/按钮高度 | `32px` |
| 输入框水平内边距 | `12px` |
| 普通按钮水平内边距 | `16px` |
| 按钮内部文字与图标间距 | `4px` |
| 筛选操作按钮间距 | `8px` |
| 工具栏左右区域间距 | `12px` |
| 工具栏按钮间距 | `4px` |
| 工具栏与表格间距 | `12px` |
| 表头高度 | `40px` |
| 表头单元格水平内边距 | `12px` |
| 业务列宽 | `240px` |
| 表格边框 | `1px` |
| 空状态区内边距 | `20px` |
| 空状态图片 | `100px × 100px` |
| 空状态图文间距 | `8px` |
| 分页区高度 | `64px` |
| 分页区内边距 | `12px 16px` |
| 分页主分组间距 | `8px` |
| 页码间距 | `2px` |
| 页码尺寸 | `24px × 24px` |
| 每页条数控件内边距 | `2px 8px` |
| 跳页输入框宽度 | `40px` |

## 字体与颜色

- 默认字体：`PingFang SC`。
- 表单、按钮、表头、空状态：`14px / 22px`。
- 字段标签：`14px / 20px`，颜色 `#505266`。
- 表头：`14px / 22px / 600`，背景 `#FAFAFA`。
- 空状态文字：`14px / 22px`，颜色 `#737585`。
- 分页：`12px / 20px`。
- 主色：`#333FFF`。
- 一级边框：`#DCDCE0`。
- 表格边框：`#EEEEF0`。
- 筛选区背景：`linear-gradient(180deg, #FAFAFC 0%, rgba(255,255,255,0) 100%)`。

## 空数据行为

- 数据为空时仍展示完整表头，不隐藏表格结构。
- 空状态在表格剩余内容区水平、垂直居中。
- 本模板仍展示分页条；总数和页码是模板占位数据，业务实现必须使用真实数据。
- 空状态不附带创建按钮或说明文案，只有图片和“暂无数据”。
